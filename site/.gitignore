# The repository root's .gitignore covers env files, secrets, node_modules and build output for this folder
# too; this file only restates what is specific to the site, for anyone who opens site/ on its own.
node_modules/
.next/
out/
next-env.d.ts
storybook-static/
# Built from zengin/tokens*.json by `zengin tokens`
src/styles/generated/
.env
.env.*
!.env.example
.vercel/
