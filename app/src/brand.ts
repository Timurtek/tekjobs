/** The product's name and mark. Import it wherever the product names itself. The mark is the illustrated character, the same one on the site and the npm page; the lime T (favicon.svg) stays as the secondary monogram, used where a badge needs a glyph. */
export const brand = {
  name: "TekJobs",
  logo: "/brand/tekjobs-avatar-192.png",
} as const;
